Closing Task 16 — AI Platform Services. All sub-tasks complete.

16.1 MinIO — Deployed across all three environments (pai-dev, pai-test, pai-prod). ArgoCD managed with self-healing. Credentials in Vault via ESO. LoadBalancer and Gateway API console routes configured.

16.2 Milvus — Deployed across all three environments. ArgoCD managed. Standalone mode with embedded etcd and emptyDir overlay for NFS compatibility. Per-environment MinIO rootPath isolation. Prod issue resolved (root cause was MTU 1500 on Nexus SVIs, corrected to 9216).

16.3 CNPG — Operator installed (v1.28.1) via OperatorHub. Validated with a 3-instance HA PostgreSQL cluster. Automatic failover tested and confirmed. Credentials managed autonomously by CNPG. Template G documented for app team self-service.

16.4 ESO in ArgoCD — SecretStores deployed in 5 namespaces (pai-dev, pai-test, pai-prod, trident, ldap-group-sync). ExternalSecrets and Vault egress NetworkPolicy consolidated into eso ArgoCD app.

16.5 NetworkPolicies — Default-deny cross-namespace policies enforced in all three environments. Allows same-namespace, ingress, and monitoring traffic. ArgoCD managed.

16.6 Vault LDAP — Integrated with Red Hat IdM. Two policies: engops-admin (full access) and mlops-app (scoped to kv/pai-dev/*, kv/pai-test/*, kv/pai-prod/*).

16.7 LDAP cluster access — LDAP group sync CronJob runs every 30 minutes from Red Hat IdM. Tiered RBAC enforced: engops=cluster-admin, mlops=admin (dev), edit (test), view (prod). htpasswd identity provider retired. OAuth config and group sync managed via ArgoCD (authentication/ directory).

16.8 Onboarding templates — v2.2 published with 8 templates (A through H): HTTPRoutes, LoadBalancers, TLS certificates, persistent storage, MinIO/Milvus/PostgreSQL connectivity, and External Secrets. Published to Confluence.

16.9 Operations runbook — Complete. Covers health checks, ArgoCD operations, storage, networking, Vault/ESO, authentication/RBAC, operators, infrastructure services, MachineConfigs, NMState, TLS, troubleshooting playbooks, and emergency procedures. Published to Confluence.

16.10 Triton validation — First AI workload (Triton inference server) deployed in pai-dev. Resolved MinIO PVC mount issue with init container pattern and MC_CONFIG_DIR fix for OpenShift restricted SCC. Documented in Template F.

All platform services operational. Documentation published to Confluence. Task closed.



Hey team, heads up on a login change for the OCP-AI cluster.

We've moved authentication to Red Hat IdM (LDAP). The old htpasswd accounts have been removed. Going forward, use your IPA credentials to log in.

Web Console: https://console-openshift-console.apps.ocp-ai.calix.local
Select "ldap-ipa" on the login page and use your IPA username and password.

CLI:
oc login -u <your-ipa-username> https://api.ocp-ai.calix.local:6443 --insecure-skip-tls-verify

Vault:
https://vault.calix.local:8200
Select LDAP and use the same IPA credentials.

If you're not sure what your IPA username is, it's the short form (e.g. awkumar, ajayacha, sulsinha) not the old format (e.g. awanish.kumar).

Let me know if you hit any issues.
