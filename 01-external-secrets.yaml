One tweak from the original approach. I’d avoid using the cpeg group since it’s a people group and not tied to this workflow. The directory is already owned by sw and builder is already a member there, so adding www-data to sw and enabling group write keeps everything aligned without impacting the existing build process.
