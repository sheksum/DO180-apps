# Show all keys owned by UID 13232 with their type and description
sudo awk '$5 == 13232' /proc/keys | head -50
sudo awk '$5 == 13232' /proc/keys | wc -l

# Any running processes owned by hsuma?
ps -u hsuma -o pid,ppid,etime,cmd

# Any lingering systemd user instance?
loginctl list-users
