hsuma@plnx-admin:~$
hsuma@plnx-admin:~$ oc set env deploy/internal-agentmesh-agent-gateway-service -n pai-dev --list
# deployments/internal-agentmesh-agent-gateway-service, container internal-agentmesh-agent-gateway-service
PGSSLMODE=disable
XDG_CACHE_HOME=/tmp/.cache
UV_CACHE_DIR=/tmp/uv-cache
# DATABASE_USER from secret db-credentials, key DATABASE_USER
# DATABASE_PASSWORD from secret db-credentials, key DATABASE_PASSWORD
POSTGRES_ASYNC_DB_URL=postgresql+asyncpg://$(DATABASE_USER):$(DATABASE_PASSWORD)@postgresql:5432/test_db
POSTGRES_DB_URL=postgresql+asyncpg://$(DATABASE_USER):$(DATABASE_PASSWORD)@postgresql:5432/test_db
# ATLASSIAN_CLIENT_ID from secret atlassian-secret, key ATLASSIAN_CLIENT_ID
# ATLASSIAN_CLIENT_SECRET from secret atlassian-secret, key ATLASSIAN_CLIENT_SECRET
# FERNET_SECRET from secret fernet-secret, key FERNET_SECRET
hsuma@plnx-admin:~$
