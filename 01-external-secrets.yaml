# Does the host even know pln-ipamaster exists?
dig +short pln-ipamaster.calix.local
dig +short pln-ipamaster.ipa.calix.local

# What's in resolv.conf?
cat /etc/resolv.conf

# Are the two configured servers reachable?
dig +short cpeg-ipareplica.ipa.calix.local
dig +short pln-petipareplica.ipa.calix.local
