# How often is SSSD on cpeg going offline? Count offline events in the last hour.
sudo grep -c "Going offline" /var/log/sssd/sssd_ipa.calix.local.log

# And in the last day, when does it happen?
sudo grep "Going offline" /var/log/sssd/sssd_ipa.calix.local.log | tail -20
