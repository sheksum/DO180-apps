rpm -qa | grep -E "ipa-server|389-ds-base|pki-server"
ls /etc/dirsrv/ /var/lib/ipa/ 2>/dev/null
dig +short pln-ipamaster.ipa.calix.local
dig +short -x $(hostname -i)
chronyc sources

dnf install -y ipa-server ipa-server-dns


ipa-client-install --domain=ipa.calix.local --realm=IPA.CALIX.LOCAL \
  --server=cpeg-ipareplica.ipa.calix.local --principal=admin --mkhomedir --no-ntp

  ipa-replica-install --setup-dns --no-forwarders --no-ntp --principal=admin

  ipactl status
ipa-replica-manage list
