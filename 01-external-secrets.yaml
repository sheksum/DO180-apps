oc exec -n pai-dev deploy/internal-agentmesh-agent-gateway-service -- nslookup agentmesh.dev.ocp-ai.calix.local
```

It's probably failing because the cluster's internal DNS (CoreDNS) doesn't know about the external `*.dev.ocp-ai.calix.local` wildcard — that's resolved by the corporate DNS, and the pod might not have access to it.

Two fixes:

**Option 1 (correct approach):** Change the env var to use the internal service name. Since both pods are in pai-dev, use:
```
http://internal-agentmesh-generic-agent:9999
