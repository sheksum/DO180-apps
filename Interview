# Check for any errors in the log
grep -i "error\|fail\|invalid" /tmp/expired_certs_delete.log | head -20

# Confirm EXPIRED and REVOKED_EXPIRED are gone
ldapsearch -Y EXTERNAL -H ldapi://%2Frun%2Fslapd-IPA-CALIX-LOCAL.socket \
  -b "ou=certificateRepository,ou=ca,o=ipaca" \
  -s one "(objectclass=*)" certStatus \
  2>/dev/null | grep "^certStatus:" | sort | uniq -c | sort -rn


[root@cpeg-ipareplica ~]#
[root@cpeg-ipareplica ~]# ldapsearch -Y EXTERNAL -H ldapi://%2Frun%2Fslapd-IPA-CALIX-LOCAL.socket \
>   -b "ou=certificateRepository,ou=ca,o=ipaca" \
>   -s one "(objectclass=*)" certStatus \
>   2>/dev/null | grep "^certStatus:" | sort | uniq -c | sort -rn
 857285 certStatus: VALID
      1 certStatus: REVOKED
[root@cpeg-ipareplica ~]#
