# On cpeg - enable ignore-time-skew on the replication agreement to pln-ipamaster
# First let's clean the stale pln-ipamaster replication agreements
ipa-replica-manage del pln-ipamaster.ipa.calix.local --force 2>/dev/null
ipa-csreplica-manage del pln-ipamaster.ipa.calix.local --force 2>/dev/null
