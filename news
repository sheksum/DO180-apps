# On cpeg
ldapsearch -Y GSSAPI -b "cn=masters,cn=ipa,cn=etc,dc=ipa,dc=calix,dc=local" \
  "(cn=pln-ipamaster.ipa.calix.local)" dn 2>/dev/null


# On pln
cat /var/lib/ipa/sysrestore/sysrestore.state | head -50
