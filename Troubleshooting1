hsuma@plnx-admin:~$ for node in ocp-ai-master01.calix.local ocp-ai-master02.calix.local ocp-ai-master03.calix.local; do
  echo "=== $node ==="
  oc debug node/$node -- chroot /host bash -c '
    if ! iptables -t nat -C POSTROUTING -m conntrack --ctstate DNAT -o br-ex -j MASQUERADE 2>/dev/null; then
      iptables -t nat -I POSTROUTING -m conntrack --ctstate DNAT -o br-ex -j MASQUERADE
      echo "MASQUERADE rule added"
    else
      echo "MASQUERADE rule already exists"
    fi
  '
done
=== ocp-ai-master01.calix.local ===
Starting pod/ocp-ai-master01calixlocal-debug-6p4v5 ...
To use host binaries, run `chroot /host`. Instead, if you need to access host namespaces, run `nsenter -a -t 1`.
MASQUERADE rule already exists

Removing debug pod ...
=== ocp-ai-master02.calix.local ===
Starting pod/ocp-ai-master02calixlocal-debug-5vgmn ...
To use host binaries, run `chroot /host`. Instead, if you need to access host namespaces, run `nsenter -a -t 1`.
MASQUERADE rule added

Removing debug pod ...
=== ocp-ai-master03.calix.local ===
Starting pod/ocp-ai-master03calixlocal-debug-75glq ...
To use host binaries, run `chroot /host`. Instead, if you need to access host namespaces, run `nsenter -a -t 1`.
MASQUERADE rule added

Removing debug pod ...
hsuma@plnx-admin:~$ oc debug node/ocp-ai-master02.calix.local -- chroot /host bash -c '
curl -m5 -s -o /dev/null -w "HTTP status: %{http_code}\n" http://10.172.194.160:9000
'
Starting pod/ocp-ai-master02calixlocal-debug-j6cs9 ...
To use host binaries, run `chroot /host`. Instead, if you need to access host namespaces, run `nsenter -a -t 1`.
HTTP status: 000

Removing debug pod ...
error: non-zero exit code from debug container
hsuma@plnx-admin:~$
