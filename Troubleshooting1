# 1. Full unfiltered speaker logs — we need to see EVERYTHING, not just greps
oc logs -n metallb-system speaker-96xwt -c speaker

# 2. Also the FRR container (second container in the speaker pod)
oc logs -n metallb-system speaker-96xwt -c frr

# 3. And check — does the speaker actually see the node's interfaces?
#    (This is what the speaker uses to match against L2Advertisement.interfaces)
ssh -i ~/.ssh/ocp-ai core@10.172.248.150 "ip -o link show | grep -i bnd0"
