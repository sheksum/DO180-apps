# 1. Current L2Advertisements — what's the interfaces field?
oc get l2advertisement -n metallb-system -o yaml

# 2. Full YAML of one ServiceL2Status — are we using the wrong field names?
oc get servicel2status -n metallb-system -o yaml | head -60

# 3. IPAddressPools
oc get ipaddresspool -n metallb-system -o yaml

# 4. Full speaker log from one speaker to see the announce logic
oc logs -n metallb-system -l component=speaker --tail=50 2>&1 | head -80
