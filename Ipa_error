cat > /tmp/clean56.ldif <<'EOF'
dn: cn=clean 56, cn=cleanallruv, cn=tasks, cn=config
objectclass: extensibleObject
replica-base-dn: dc=ipa,dc=calix,dc=local
replica-id: 56
replica-force-cleaning: yes
cn: clean 56
EOF

sudo ldapmodify -Y EXTERNAL -H ldapi://%2Frun%2Fslapd-IPA-CALIX-LOCAL.socket -a -f /tmp/clean56.ldif

sudo ldapsearch -Y EXTERNAL -H ldapi://%2Frun%2Fslapd-IPA-CALIX-LOCAL.socket \
  -b "cn=clean 56,cn=cleanallruv,cn=tasks,cn=config" \
  nsTaskCurrentItem nsTaskStatus nsTaskExitCode 2>&1 | grep -iE "^dn:|nsTask"
