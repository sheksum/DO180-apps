# Check what cleanallruv tasks are currently running (should be empty)
sudo ldapsearch -Y EXTERNAL -H ldapi://%2Frun%2Fslapd-IPA-CALIX-LOCAL.socket \
  -b "cn=cleanallruv,cn=tasks,cn=config" dn 2>&1 | grep -i "^dn:"

# Check what abort-cleanallruv tasks exist
sudo ldapsearch -Y EXTERNAL -H ldapi://%2Frun%2Fslapd-IPA-CALIX-LOCAL.socket \
  -b "cn=abort cleanallruv,cn=tasks,cn=config" dn 2>&1 | grep -i "^dn:"
