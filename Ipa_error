# On cpeg
ss -tan | awk '$4 ~ /:389$/ {print $1}' | sort | uniq -c
ps -p $(pidof ns-slapd) -o pid,%cpu,%mem,rss,nlwp,stat
# Watch for slow ops in real-time (Ctrl-C to stop)
tail -f /var/log/dirsrv/slapd-IPA-CALIX-LOCAL/access | grep -E "etime=[1-9]\."


sudo sssctl domain-status ipa.calix.local
sudo grep -c "SSSD is offline" /var/log/sssd/sssd_ipa.calix.local.log
sudo grep -c "SSSD is offline" /var/log/sssd/sssd_ipa.calix.local.log.1 2>/dev/null
# Compare to yesterday
sudo ls -la /var/log/sssd/ | grep -E "^[^d]"
