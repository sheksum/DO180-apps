# On cpeg - check for any references to replica 52 in replication agreements
sudo dsconf -D "cn=Directory Manager" ldap://cpeg-ipareplica.ipa.calix.local \
  repl-agmt list --suffix="dc=ipa,dc=calix,dc=local" 2>&1 | grep -E "^nsds50ruv|replica 52|replica 56"
