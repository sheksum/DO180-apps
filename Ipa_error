# Size of the gidNumber index vs expected
ls -lah /var/lib/dirsrv/slapd-IPA-CALIX-LOCAL/db/userRoot/gidNumber.db*
ls -lah /var/lib/dirsrv/slapd-IPA-CALIX-LOCAL/db/userRoot/id2entry.db*

# Do a direct ldapsearch matching the slow filter and see what etime comes back
time ldapsearch -x -H ldap://localhost -D "cn=Directory Manager" -W \
  -b "cn=accounts,dc=ipa,dc=calix,dc=local" \
  "(&(gidNumber=2200)(|(objectClass=ipaUserGroup)(objectClass=posixGroup))(cn=*)(&(gidNumber=*)(!(gidNumber=0))))" \
  dn 2>&1 | tail -5


grep -i enumerate /etc/sssd/sssd.conf
