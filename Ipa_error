sudo dsconf -D "cn=Directory Manager" ldap://cpeg-ipareplica.ipa.calix.local \
  repl-agmt list --suffix="o=ipaca" 2>&1 | grep -E "^dn:|nsds5replicaHost"

# From cpeg or anywhere reachable
nc -zv pln-ipamaster.ipa.calix.local 389 2>&1
ssh root@pln-ipamaster "sudo systemctl is-active dirsrv@*" 2>&1
