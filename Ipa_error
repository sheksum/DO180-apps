# On cpeg
jobs
ps -ef | grep -E "tail|journalctl|wall" | grep -v grep
grep -E "^(ForwardToConsole|MaxLevelConsole|TTYPath)" /etc/systemd/journald.conf
# Also what's writing to your pty
fuser /dev/$(tty | sed 's|/dev/||')  2>/dev/null
who -a | head -10
