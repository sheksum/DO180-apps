# What's the state of the clean 57 task?
sudo ldapsearch -Y EXTERNAL -H ldapi://%2Frun%2Fslapd-IPA-CALIX-LOCAL.socket \
  -b "cn=clean 57,cn=cleanallruv,cn=tasks,cn=config" 2>&1 | grep -v "^$"

# Also check what the abort-cleanallruv container has
sudo ldapsearch -Y EXTERNAL -H ldapi://%2Frun%2Fslapd-IPA-CALIX-LOCAL.socket \
  -b "cn=abort cleanallruv,cn=tasks,cn=config" 2>&1 | grep -v "^$" | head -30
