cat > /tmp/clean52.ldif <<'EOF'
dn: cn=clean 52, cn=cleanallruv, cn=tasks, cn=config
objectclass: extensibleObject
replica-base-dn: dc=ipa,dc=calix,dc=local
replica-id: 52
replica-force-cleaning: yes
cn: clean 52
EOF

sudo ldapmodify -Y EXTERNAL -H ldapi://%2Frun%2Fslapd-IPA-CALIX-LOCAL.socket -a -f /tmp/clean52.ldif


sudo ldapsearch -Y EXTERNAL -H ldapi://%2Frun%2Fslapd-IPA-CALIX-LOCAL.socket \
  -b "cn=clean 52,cn=cleanallruv,cn=tasks,cn=config" \
  nsTaskCurrentItem nsTaskStatus nsTaskExitCode 2>&1 | grep -iE "^dn:|nsTask"
