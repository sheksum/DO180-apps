# Check 1: Any tasks currently running?
sudo ldapsearch -Y EXTERNAL -H ldapi://%2Frun%2Fslapd-IPA-CALIX-LOCAL.socket \
  -b "cn=cleanallruv,cn=tasks,cn=config" nsTaskCurrentItem nsTaskStatus 2>&1 | grep -iE "^dn:|nsTask"


# Check 2: Replication agreement status for nan and ric (green?)
sudo dsconf -D "cn=Directory Manager" ldap://cpeg-ipareplica.ipa.calix.local \
  repl-agmt list --suffix="dc=ipa,dc=calix,dc=local" 2>&1 | \
  grep -E "^dn:|nsds5replicaLastUpdateStatus:" | grep -v sjc
