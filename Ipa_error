# What does the log say this time?
sudo tail -50 /var/log/sssd/sssd_ipa.calix.local.log

# Is cpeg currently reachable?
time ldapsearch -x -H ldap://cpeg-ipareplica.ipa.calix.local -s base -b "" "(objectclass=*)" 2>&1 | tail -3

uptime
ss -s | head -5
sudo grep -c "ERR" /var/log/dirsrv/slapd-IPA-CALIX-LOCAL/errors | head -1
