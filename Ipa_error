grep "conn=3771795" /var/log/dirsrv/slapd-IPA-CALIX-LOCAL/access | head -5

# From cpeg itself
time ldapsearch -x -H ldap://cpeg-ipareplica.ipa.calix.local -s base -b "" supportedLDAPVersion 2>&1 | tail -3

# Current listen backlog and active conns on 389
ss -tan | awk '$4 ~ /:389$/ {print $1}' | sort | uniq -c
