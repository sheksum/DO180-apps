# Did the HTTP principal get created?
ipa service-find HTTP/pln-ipamaster.ipa.calix.local 2>&1

# Is there a master entry for pln-ipamaster?
ipa-replica-manage list 2>&1

# Check the install log tail

sudo tail -200 /var/log/ipareplica-install.log | grep -iE "error|warn|keytab|http|sasl|acl|insufficient"
