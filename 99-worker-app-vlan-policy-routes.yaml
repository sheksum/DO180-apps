oc describe mcp worker | grep -A 5 "Degraded"


oc get mcp worker -o jsonpath='{.status.conditions[?(@.type=="Degraded")]}'


oc get mcp worker -o jsonpath='{.status.conditions[?(@.type=="RenderDegraded")]}'


# From plnx-admin — test gpu01 all three VLANs
ping -c 3 10.172.194.153
ping -c 3 10.172.198.153
ping -c 3 10.172.202.153


ssh core@ocp-ai-gpu01.calix.local
journalctl -u app-vlan-policy-routes
ip rule show
ip route show table 100
ip route show table 101
ip route show table 102
