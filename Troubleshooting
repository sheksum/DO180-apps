oc get deployments -n pai-dev -o yaml | grep -A10 "affinity\|nodeSelector\|nodeName"
oc get deployments -n pai-test -o yaml | grep -A10 "affinity\|nodeSelector\|nodeName"
oc get deployments -n pai-prod -o yaml | grep -A10 "affinity\|nodeSelector\|nodeName"


oc get pods -n pai-dev -o wide
oc get pods -n pai-test -o wide
oc get pods -n pai-prod -o wide


for ip in 150 151 152; do
  echo "=== master $(($ip - 149)) ==="
  ssh -i ~/.ssh/ocp-ai core@10.172.248.$ip "ip -br addr show | grep bnd0"
done
