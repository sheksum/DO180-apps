oc delete cluster cnpg-test -n pai-test

# Verify the patch is there
oc get tridentbackendconfig backend-app-nfs -n trident -o jsonpath='{.spec.nfsMountOptions}'
# Should show: nfsvers=3

# Also check the backend itself picked it up
oc get tridentbackend tbe-zg42j -n trident -o jsonpath='{.spec.defaults}' 


oc get storageclass netapp-app -o yaml | grep -A5 mountOptions


oc patch storageclass netapp-app -p '{"mountOptions":["nfsvers=3"]}'
