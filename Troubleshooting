# Find the primary
oc get pods -n pai-test -l cnpg.io/cluster=cnpg-test -L role

# Delete the primary
oc delete pod cnpg-test-1 -n pai-test

# Watch failover
oc get pods -n pai-test -l cnpg.io/cluster=cnpg-test -o wide -w


oc run pg-test2 --rm -it --image=postgres:17 --restart=Never -n pai-test -- \
  psql "host=cnpg-test-rw port=5432 dbname=testdb user=testuser password=$PGPASS sslmode=require" -c "SELECT * FROM test;"
