ipa cert-find --status=VALID --all 2>/dev/null | grep "Number of entries"

ipa cert-find --status=VALID 2>/dev/null | grep "Serial number:" | tail -5
ipa cert-find --status=VALID 2>/dev/null | grep "Subject:" | head -20
