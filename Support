# Check what certs have been issued and are still valid
ipa cert-find --validity-notafter-from="$(date +%Y-%m-%d)" 2>/dev/null | grep -E "Subject:|Serial|Status" | head -40


ipa cert-find --status=VALID 2>/dev/null | grep "Number of entries"
