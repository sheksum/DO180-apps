Brian,
Franklyn’s plan works. Three quick adds: set vm.swappiness=10 so swap is a safety net not primary, regenerate the swap UUID with mkswap on cloned VMs so fstab entries don’t collide, and update the SDAXOS template with the second disk so new VMs deploy with the right swap config from day one.
Thanks,
Haj