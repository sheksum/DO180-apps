Brian Wing  [2:26 PM]
@gene.siepka I'm not sure what we'll need to address this with Openshift K8S, but need to evaluate https://copy.fail/
XintCopy Fail — 732 Bytes to RootCVE-2026-31431. 100% Reliable Linux LPE — no race, no per-distro offsets, page-cache write that bypasses on-disk file-integrity tools and crosses containers. Found by Xint Code.https://copy.fail/Gene Siepka  [2:34 PM]
Openshift nodes are RHCOS, not RHEL Proper, but still affected.   RHCOS is like immutable OS, only changes can be applied via code / reboot..  @haj.suma correct me if I'm wrong.

We'd have to apply this MachineConfig and reboot each node. (courtesy of /r/openshift)

apiVersion: machineconfiguration.openshift.io/v1
kind: MachineConfig
metadata:
  name: 99-mitigate-cve-2026-31431-master
  labels:
    machineconfiguration.openshift.io/role: master
spec:
  config:
    ignition:
      version: 3.4.0
  kernelArguments:
    - initcall_blacklist=algif_aead_init
---
apiVersion: machineconfiguration.openshift.io/v1
kind: MachineConfig
metadata:
  name: 99-mitigate-cve-2026-31431-worker
  labels:
    machineconfiguration.openshift.io/role: worker
spec:
  config:
    ignition:
      version: 3.4.0
  kernelArguments:
    - initcall_blacklist=algif_aead_init
