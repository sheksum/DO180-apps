Brian,
Franklyn’s plan works. Three quick adds: set vm.swappiness=10 so swap is a safety net not primary, use a filesystem label in fstab (not UUID) so cloned templates don’t break, and bake the second disk into the SDAXOS template so new builds ship correct.
Thanks,
Haj