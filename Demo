oc get clusterrolebinding cluster-admin-0 -o yaml | grep -A10 "subjects:"
oc get clusterrolebinding cluster-admin-1 -o yaml | grep -A10 "subjects:"
oc get clusterrolebinding cluster-admins -o yaml | grep -A10 "subjects:"
