cat > ~/ocp-ai-config/network-policies/environment-isolation.yaml << 'EOF'
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-cross-namespace
  namespace: pai-dev
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              network.openshift.io/policy-group: ingress
    - from:
        - namespaceSelector:
            matchLabels:
              network.openshift.io/policy-group: monitoring
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-cross-namespace
  namespace: pai-test
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              network.openshift.io/policy-group: ingress
    - from:
        - namespaceSelector:
            matchLabels:
              network.openshift.io/policy-group: monitoring
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-cross-namespace
  namespace: pai-prod
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              network.openshift.io/policy-group: ingress
    - from:
        - namespaceSelector:
            matchLabels:
              network.openshift.io/policy-group: monitoring
EOF



cd ~/ocp-ai-config
git add network-policies/ apps/network-policies.yaml
git commit -m "Add NetworkPolicies for environment isolation with monitoring access"
git push


# Cross-namespace should FAIL
oc run test-isolation --rm -it --image=busybox --restart=Never -n pai-dev -- wget -qO- --timeout=3 http://milvus.pai-test.svc:9091/healthz

# Same namespace should WORK
oc run test-isolation --rm -it --image=busybox --restart=Never -n pai-dev -- wget -qO- --timeout=3 http://milvus.pai-dev.svc:9091/healthz
