# Check which binding is which
oc get rolebinding admin -n pai-dev -o jsonpath='{.subjects[*].name}'
oc get rolebinding admin-0 -n pai-dev -o jsonpath='{.subjects[*].name}'

# Check engops cluster-admin
oc get clusterrolebindings | grep -i "engops\|cluster-admin-1"


# Remove orphaned old bindings
oc delete rolebinding admin -n pai-dev
oc delete rolebinding edit -n pai-test
oc delete rolebinding view -n pai-prod


oc auth can-i --list --as="" --as-group=engops | grep -i "cluster\|admin" | head -5
