# Check speaker DaemonSet node selector
oc get daemonset -n metallb-system speaker -o yaml | grep -A5 "nodeSelector\|tolerations" | head -20

# Check if speakers should be on GPU workers too
oc get nodes --show-labels | grep -i metallb


# Check speaker DaemonSet node selector
oc get daemonset -n metallb-system speaker -o yaml | grep -A5 "nodeSelector\|tolerations" | head -20

# Check if speakers should be on GPU workers too
oc get nodes --show-labels | grep -i metallb
