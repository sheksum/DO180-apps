oc logs -n pai-dev deployment/internal-agentmesh-generic-agent --tail=50
oc describe pod -n pai-dev -l app=internal-agentmesh-generic-agent | grep -A10 "Liveness\|Events"
```

## 2. TTS service crash-looping
`internal-agentmesh-tts-service` is back-off restarting — check if agentmesh depends on it for responses.

## 3. Docker Hub rate limit (not urgent but noisy)
```
toomanyrequests: You have reached your unauthenticated pull rate limit
```
`busybox:latest` and `minio/mc:latest` are being pulled directly from Docker Hub unauthenticated. These should be proxied through your Nexus instance. Tell Awanish — these init containers need to reference `thirdparty-nexus.tools.shared.xilac.net` instead of pulling from Docker Hub directly.

## 4. Nexus auth failure
```
registry.friendli.ai/calix:v1.13.28 unauthorized
