# Can you ping the gateway?
ping -c 3 10.172.194.1

# Can you ping the node's VLAN IP?
ping -c 3 10.172.194.151

# Can you reach MinIO internally via the management IP?
curl -s -o /dev/null -w "%{http_code}" http://10.172.248.151:9000

# Check plnx-admin's routing table
ip route show | grep 10.172.194
