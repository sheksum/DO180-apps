
hsuma@plnx-admin:~$

# Is MinIO's LoadBalancer service still assigned?
oc get svc minio-lb -n pai-dev

# Check MetalLB speaker pods
oc get pods -n metallb-system

# Check which node is announcing the VIP
oc get svc minio-lb -n pai-dev -o yaml | grep -A5 "status:"

# Try reaching the MetalLB VIP directly
arping -I eth0 10.172.194.160 -c 3

# Check if the VIP responds to ARP
ping -c 3 10.172.194.160
