# Check for memberlist secret
oc get secret -n metallb-system | grep member

# Check the MetalLB CR for memberlist config
oc get metallb metallb -n metallb-system -o yaml

# Check if speakers can reach each other directly
oc exec -n metallb-system $(oc get pods -n metallb-system -l component=speaker -o name | head -1) -c speaker -- wget -qO- --timeout=2 http://10.172.248.151:7946 2>&1
oc exec -n metallb-system $(oc get pods -n metallb-system -l component=speaker -o name | head -1) -c speaker -- wget -qO- --timeout=2 http://10.172.248.152:7946 2>&1
