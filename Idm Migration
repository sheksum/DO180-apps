# On pln-ipamaster
certutil -L -d /etc/dirsrv/slapd-IPA-CALIX-LOCAL/ | grep -i "IPA\|CA"

# Also check if pki-tomcat NSS db trusts the DS cert
certutil -L -d /var/lib/pki/pki-tomcat/conf/alias/ | grep -i "IPA\|CA"

systemctl status pki-tomcatd@pki-tomcat --no-pager | grep -E "Active|SEVERE|failed"
