# 1. Verify CA is issuing certs
ipa cert-show 1

# 2. Check replication status to cpeg
ipa-replica-manage list

# 3. Verify certmonger tracking
getcert list | grep -E "status|subject" | head -30

# 4. Check topology
ipa topologysegment-find dc=ipa,dc=calix,dc=local 2>/dev/null
