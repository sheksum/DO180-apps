# Test from a pod on the cluster - does the LB work internally?
oc run lb-test --rm -it --image=busybox --restart=Never -n pai-dev -- wget -qO- --timeout=3 http://10.172.194.160:9000/minio/health/live

# Test from a master node directly
ssh -i ~/.ssh/ocp-ai core@ocp-ai-master01.calix.local "curl -s -o /dev/null -w '%{http_code}' --connect-timeout 3 http://10.172.194.160:9000/minio/health/live"
