# On pln-ipamaster
firewall-cmd --permanent --add-service={dns,http,https,kadmin,kerberos,kpasswd,ldap,ldaps,nfs,ntp,ssh,cockpit}
firewall-cmd --reload
firewall-cmd --list-all
```

Also notice `cpeg` has two extra ports open:
```
514/tcp   — syslog
1517/tcp  — not standard, possibly custom

dnf module enable idm:DL1 -y
dnf module install idm:DL1/server -y
