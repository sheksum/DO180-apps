# Check if speakers see the L2Advertisement config
oc logs -n metallb-system $(oc get pods -n metallb-system -l component=speaker -o name | head -1) -c speaker 2>&1

# Check controller logs for L2 processing
oc logs -n metallb-system -l component=controller --all-containers --tail=50 | grep -i "l2\|advertisement\|assign\|error"

# Check if the issue is specific to the interfaces field in L2Advertisement
# Try removing the interfaces constraint temporarily
oc get l2advertisement l2-vlan1794-dev -n metallb-system -o yaml


oc patch subscription metallb-operator -n metallb-system --type merge -p '{"spec":{"installPlanApproval":"Manual"}}'
