# On pln
ldapsearch -Y EXTERNAL -H ldapi://%2Frun%2Fslapd-IPA-CALIX-LOCAL.socket \
  -b "cn=masters,cn=ipa,cn=etc,dc=ipa,dc=calix,dc=local" \
  "(objectclass=*)" dn 2>/dev/null

grep -v DEBUG /var/log/ipareplica-install.log | tail -20
