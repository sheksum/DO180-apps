ldapsearch -H ldap://cpeg-ipareplica.ipa.calix.local \
  -x -D "cn=Directory Manager" -W \
  -b "cn=config" \
  "(cn=*pln-ipamaster*)" dn nsds5replicaTimeout nsds5replicaConnectionTimeout

# Also check o=ipaca replication agreements
ldapsearch -H ldap://cpeg-ipareplica.ipa.calix.local \
  -x -D "cn=Directory Manager" -W \
  -b "cn=replica,cn=o\3Dipaca,cn=mapping tree,cn=config" \
  "(objectclass=nsds5replicationagreement)" dn nsds5replicaTimeout


systemctl status pki-tomcatd@pki-tomcat
ss -tlnp | grep 36389
