# List all ServiceL2Status objects
oc get servicel2statuses.metallb.io -A

# Delete the stale ones
oc delete servicel2statuses.metallb.io l2-9smz4 -n pai-test
oc delete servicel2statuses.metallb.io l2-4vfpl -n openshift-ingress
oc delete servicel2statuses.metallb.io l2-z8xc8 -n pai-prod
oc delete servicel2statuses.metallb.io l2-l4jkn -n pai-prod

# Restart the speakers to force re-announcement
oc delete pods -n metallb-system -l component=speaker

# Wait for speakers to come back
oc get pods -n metallb-system -l component=speaker -w

# Test
ping -c 3 10.172.194.160
