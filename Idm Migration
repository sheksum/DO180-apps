# What happened today with MetalLB?
oc get events -n metallb-system --sort-by='.lastTimestamp' | tail -30

# Check if the operator auto-upgraded
oc get installplan -n metallb-system

# Check the previous CSV version
oc get csv -n metallb-system -o yaml | grep -i "replaces\|version\|createdAt"
