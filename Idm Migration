# On cpeg-ipareplica
grep -i timeout /etc/pki/pki-tomcat/ca/CS.cfg | head -20

# Also check the CA DS replication agreement timeout
ldapsearch -H ldap://cpeg-ipareplica.ipa.calix.local \
  -x -D "cn=Directory Manager" -W \
  -b "cn=mapping tree,cn=config" \
  "(cn=*pki*)" nsds5replicaTimeout nsds5replicaConnectionTimeout
