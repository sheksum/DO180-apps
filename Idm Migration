NEW_PWD="<cpeg-dm-password>"

ldapmodify -H ldapi://%2Frun%2Fslapd-IPA-CALIX-LOCAL.socket \
  -Y EXTERNAL <<EOF
dn: cn=Directory Manager
changetype: modify
replace: userPassword
userPassword: ${NEW_PWD}
EOF
