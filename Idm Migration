# On pln-ipamaster
ipa-server-install --uninstall --ignore-topology-disconnect
```

**Issue 2 — cpeg has a serious CSN changelog problem:**
```
Can't locate CSN ffffffff6f0b00140000 in the changelog


ipa-replica-manage list -v
ipa-replica-manage csn cpeg-ipareplica.ipa.calix.local
