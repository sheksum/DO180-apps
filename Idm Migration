# Scale down the operator
oc scale deployment metallb-operator-controller-manager -n metallb-system --replicas=0

# Check what env vars the speaker has for node IP
oc exec -n metallb-system $(oc get pods -n metallb-system -l component=speaker -o name | head -1) -c speaker -- env | grep -i METALLB
