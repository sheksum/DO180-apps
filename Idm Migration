# Check if the Gateway VIP was EVER reachable today
# Is the agentmesh URL still working from plnx-admin?
curl -k -s -o /dev/null -w "%{http_code}" https://agentmesh.dev.ocp-ai.calix.local

# Check speaker errors right now
oc logs -n metallb-system -l component=speaker --all-containers --tail=20 | grep -i error

# Check the MetalLB controller
oc logs -n metallb-system deployment/controller --all-containers --tail=20 | grep -i error

# Is there a frr-k8s or FRR container issue?
oc get pods -n metallb-system
