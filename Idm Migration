kinit admin
ipa server-del pln-ipamaster.ipa.calix.local --force --ignore-topology-disconnect 2>/dev/null
ipa host-add pln-ipamaster.ipa.calix.local --force
ipa dnsrecord-add ipa.calix.local pln-ipamaster --a-rec 10.172.248.55
ipa service-add HTTP/pln-ipamaster.ipa.calix.local
ipa service-add ldap/pln-ipamaster.ipa.calix.local


cat /etc/ipa/installer.conf
grep startup_timeout /usr/lib/python3.9/site-packages/ipalib/constants.py
cat /etc/systemd/system/pki-tomcatd@pki-tomcat.service.d/timeout.conf
cat /usr/share/ipa/replica-prevent-time-skew.ldif


[root@cpeg-ipareplica sos.imb0ecrg]# ipa server-del pln-ipamaster.ipa.calix.local --force --ignore-topology-disconnect 2>/dev/null
Removing pln-ipamaster.ipa.calix.local from replication topology, please wait...
--------------------------------------------------
Deleted IPA server "pln-ipamaster.ipa.calix.local"
--------------------------------------------------
[root@cpeg-ipareplica sos.imb0ecrg]#
[root@cpeg-ipareplica sos.imb0ecrg]#
[root@cpeg-ipareplica sos.imb0ecrg]# ldapsearch -Y EXTERNAL -H ldapi://%2Frun%2Fslapd-IPA-CALIX-LOCAL.socket   -b "cn=config" "(nsds5replicaHost=pln-ipamaster.ipa.calix.local)" dn 2>/dev/null
# extended LDIF
#
# LDAPv3
# base <cn=config> with scope subtree
# filter: (nsds5replicaHost=pln-ipamaster.ipa.calix.local)
# requesting: dn
#

# meTopln-ipamaster.ipa.calix.local, replica, dc\3Dipa\2Cdc\3Dcalix\2Cdc\3Dloca
 l, mapping tree, config
dn: cn=meTopln-ipamaster.ipa.calix.local,cn=replica,cn=dc\3Dipa\2Cdc\3Dcalix\2
 Cdc\3Dlocal,cn=mapping tree,cn=config

# caTopln-ipamaster.ipa.calix.local, replica, o\3Dipaca, mapping tree, config
dn: cn=caTopln-ipamaster.ipa.calix.local,cn=replica,cn=o\3Dipaca,cn=mapping tr
 ee,cn=config

# search result
search: 2
result: 0 Success

# numResponses: 3
# numEntries: 2
[root@cpeg-ipareplica sos.imb0ecrg]#


ipa topologysegment-find dc=ipa,dc=calix,dc=local 2>/dev/null | grep pln
ipa topologysegment-find o=ipaca 2>/dev/null | grep pln
