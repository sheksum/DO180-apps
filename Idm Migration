
# Test key ports from pln-ipamaster to cpeg
for port in 88 389 443 464 636; do nc -zv cpeg-ipareplica.ipa.calix.local $port 2>&1 | grep -E "(succeeded|failed|refused)"; done


firewall-cmd --permanent --add-service={freeipa-ldap,freeipa-ldaps,freeipa-replication,dns,ntp,https,kerberos,kpasswd}
firewall-cmd --reload
firewall-cmd --list-services




[root@cpeg-ipareplica ~]# firewall-cmd --list-all
public (active)
  target: default
  icmp-block-inversion: no
  interfaces: ens192
  sources:
  services: cockpit dhcpv6-client dns http https kadmin kerberos kpasswd ldap ldaps nfs ntp ssh
  ports: 514/tcp 1517/tcp
  protocols:
  forward: no
  masquerade: no
  forward-ports:
  source-ports:
  icmp-blocks:
  rich rules:
[root@cpeg-ipareplica ~]#

dnf module enable idm:DL1 -y
dnf module install idm:DL1/server -y
