# Stop DS first
systemctl stop dirsrv@IPA-CALIX-LOCAL

# Write password to temp file to avoid bash expansion issues
printf '%s' 'YOUR_ACTUAL_PASSWORD_HERE' > /tmp/newpwd.txt

# Generate hash
pwdhash -D /etc/dirsrv/slapd-IPA-CALIX-LOCAL "$(cat /tmp/newpwd.txt)"
