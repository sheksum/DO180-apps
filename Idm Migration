# Check current firewall
firewall-cmd --list-all

# Add missing services
firewall-cmd --permanent --add-service={ldap,ldaps,kerberos,kpasswd,http,https,dns}
firewall-cmd --permanent --add-port={389/tcp,636/tcp,88/tcp,88/udp,464/tcp,464/udp,80/tcp,443/tcp}
firewall-cmd --reload
firewall-cmd --list-all

# On cpeg-ipareplica
ipa-replica-manage del pln-ipamaster.ipa.calix.local --force
