nc -zv -w 3 10.172.194.160 9000
nc -zv -w 3 10.172.194.161 19530


cat <<EOF | oc apply -f -
apiVersion: v1
kind: Service
metadata:
  name: postgresql-lb
  namespace: pai-dev
  annotations:
    metallb.universe.tf/address-pool: pai-services-dev
spec:
  type: LoadBalancer
  selector:
    deployment: postgresql
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
EOF


oc get svc postgresql-lb -n pai-dev
nc -zv -w 3 <assigned-ip> 5432
