# Segment topology for both suffixes
ipa topologysegment-find --name domain
ipa topologysegment-find --name ca

# Or iterate both suffixes properly
ipa topologysegment-find domain
ipa topologysegment-find ca


ipa topologysegment-find domain
ipa topologysegment-find ca


# Current IPA version on cpeg
ipa --version

# Verify what's running
ipactl status

# Check certmonger — confirms CA renewal tracking
getcert list | grep -E "(status|track|ca-error|Request ID)"

# Check DNA ID ranges (important — don't want range conflicts with new replica)
ipa-replica-manage dnarange-show
