# Is pki-tomcatd still running?
systemctl status pki-tomcatd@pki-tomcat --no-pager | grep Active

# Is CA responding?
curl -sk https://pln-ipamaster.ipa.calix.local:8443/ca/admin/ca/getStatus

# Check CA debug log
ls /var/log/pki/pki-tomcat/ca/
