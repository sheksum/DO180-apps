oc run fileparser-debug2 -n pai-dev --rm -it --restart=Never \
  --image=paidev-nexus.tools.shared.xilac.net/internal-datapipeline-rag-fileparser-service:1.0.0-63dbse.ca8b1a3 \
  --overrides='{
    "spec": {
      "imagePullSecrets": [{"name":"reg-secret"}],
      "securityContext": {"runAsNonRoot": true, "seccompProfile": {"type": "RuntimeDefault"}},
      "containers": [{
        "name": "fileparser-debug2",
        "image": "paidev-nexus.tools.shared.xilac.net/internal-datapipeline-rag-fileparser-service:1.0.0-63dbse.ca8b1a3",
        "command": ["/bin/sh", "-c", "sleep 300"],
        "securityContext": {"allowPrivilegeEscalation": false, "capabilities": {"drop": ["ALL"]}}
      }],
      "nodeSelector": {"nvidia.com/gpu.present": "true"},
      "tolerations": [{"key": "nvidia.com/gpu", "operator": "Equal", "value": "true", "effect": "NoSchedule"}]
    }
  }' \
  --command -- /bin/sh -c "sleep 300"


  oc exec -it fileparser-debug2 -n pai-dev -- /bin/sh -c "which parser-service 2>/dev/null; find / -name 'parser*' 2>/dev/null; cat /app/pyproject.toml 2>/dev/null; ls -la /app/"
