journalctl -u vault --since "1 min ago" | grep -i "error\|fail\|denied\|tls"

# On plnx-vault - test the API server connectivity with the K8s CA
curl --cacert /tmp/k8s-ca.pem https://api.ocp-ai.calix.local:6443/api/v1/namespaces/pai-dev/serviceaccounts/external-secrets
