oc exec -n external-secrets deploy/external-secrets -- curl -sv https://vault.calix.local:8200/v1/sys/health 2>&1 | grep -E "SSL|TLS|subject|issuer|verify"


echo $TOKEN | cut -d. -f2 | base64 -d 2>/dev/null
