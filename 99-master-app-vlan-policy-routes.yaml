# What env vars are in the secret
oc get secret agent-gateway-env -n pai-dev -o jsonpath='{.data}' | jq -r 'keys[]'

# Current crash logs
oc logs deploy/agent-gateway -n pai-dev --tail=50

# Also check the internal one
oc logs deploy/internal-agentmesh-agent-gateway-service -n pai-dev --tail=50

# And the deployment spec to see volume mounts and env setup
oc get deploy agent-gateway -n pai-dev -o yaml | grep -A5 -i ssl
oc get deploy agent-gateway -n pai-dev -o yaml | grep -A10 envFrom
