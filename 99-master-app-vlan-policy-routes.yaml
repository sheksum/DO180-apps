# Check existing Vault policies for the pai namespaces
# SSH to plnx-vault or run from plnx-admin?
# Let's check the test-secret to see the full path structure
oc get externalsecret test-secret -n pai-dev -o yaml | grep -A20 "spec:"
