# From plnx-admin, let's check if the issue is plnx-admin specific
# Can plnx-admin reach the platform ingress VIP on the same subnet?
curl -sk --connect-timeout 5 https://console-openshift-console.apps.ocp-ai.calix.local | head -5


arp -n | grep 175
