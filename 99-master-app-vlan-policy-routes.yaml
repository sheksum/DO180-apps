# Check existing ESO SecretStores and ExternalSecrets
oc get secretstores -A
oc get externalsecrets -A

# Check the existing test-secret to see the Vault path pattern
oc get externalsecret test-secret -n pai-dev -o yaml | grep -A10 "spec:"
