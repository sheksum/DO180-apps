oc get secretstore vault-store -n pai-dev -o jsonpath='{.spec.provider.vault.caBundle}' | base64 -d | head -5

oc exec -n external-secrets deploy/external-secrets -it -- curl -sk https://vault.calix.local:8200/v1/sys/health

oc rollout restart deployment -n external-secrets external-secrets
