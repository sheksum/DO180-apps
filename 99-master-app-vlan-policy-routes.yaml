oc auth can-i create tokenreviews --as=system:serviceaccount:external-secrets:external-secrets

# On plnx-vault
vault audit list
# If no audit device, check Vault server logs
journalctl -u vault --tail=20 | grep -i "permission\|login\|error"

echo $TOKEN | cut -d. -f2 | base64 -d 2>/dev/null
