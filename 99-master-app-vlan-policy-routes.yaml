# etcd logs
oc logs milvus-etcd-0 -n pai-dev --tail=20

# milvus standalone logs — look for the actual error
oc logs -n pai-dev -l app.kubernetes.io/component=standalone --tail=30 | grep -i -E "error|fail|cannot|refused" | head -10
