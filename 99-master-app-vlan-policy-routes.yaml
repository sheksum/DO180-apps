cat > gateway-api/gateway.yaml << 'EOF'
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: pai-gateway
  namespace: openshift-ingress
spec:
  gatewayClassName: openshift-default
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same

    - name: https-dev
      protocol: HTTPS
      port: 443
      hostname: "*.dev.ocp-ai.calix.local"
      tls:
        mode: Terminate
        certificateRefs:
          - name: wildcard-dev-tls-secret
            kind: Secret
      allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              env: dev

    - name: https-test
      protocol: HTTPS
      port: 443
      hostname: "*.test.ocp-ai.calix.local"
      tls:
        mode: Terminate
        certificateRefs:
          - name: wildcard-test-tls-secret
            kind: Secret
      allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              env: test

    - name: https-prod
      protocol: HTTPS
      port: 443
      hostname: "*.prod.ocp-ai.calix.local"
      tls:
        mode: Terminate
        certificateRefs:
          - name: wildcard-prod-tls-secret
            kind: Secret
      allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              env: prod
EOF


git add gateway-api/gateway.yaml
git commit -m "fix: http listener allow Same namespace for redirect route"
git push origin main
