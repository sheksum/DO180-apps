# Remove fsGroup: 1001
sed -i 's/        fsGroup: 1001/        {}/' /tmp/milvus-dev.yaml

# Remove runAsUser: 1001 but keep the rest of the container securityContext
sed -i '/            runAsUser: 1001/d' /tmp/milvus-dev.yaml

# Verify the fix
sed -n '368,390p' /tmp/milvus-dev.yaml
