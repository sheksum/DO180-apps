# Why is fileparser at 0 replicas
oc get deploy fileparser -n pai-dev -o jsonpath='{.spec.replicas}'
echo
oc get deploy fileparser -n pai-dev -o jsonpath='{.status.conditions}' | jq .

# Check if it needs a GPU (would explain 0 available)
oc get deploy fileparser -n pai-dev -o yaml | grep -A5 -i toleration
oc get deploy fileparser -n pai-dev -o yaml | grep -A3 -i resources
