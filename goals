# Run directly on aamslc4kbg01:
df -h /var/lib/longhorn /
sudo du -sh /var/lib/longhorn/models/* 2>/dev/null
sudo du -sh /var/lib/longhorn/replicas/* 2>/dev/null | head  # the REAL Longhorn data
sudo mount | grep longhorn

# On any worker:
df -h /
sudo du -sh /var/lib/kubelet/pods/*/volumes/kubernetes.io~empty-dir/* 2>/dev/null | sort -rh | head
