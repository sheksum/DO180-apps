# Find where the cluster agent phones home to
kubectl -n cattle-system get deploy cattle-cluster-agent -o yaml | grep -E 'CATTLE_SERVER|value:' | head

# Also — the rancher "imported" reference
kubectl get clusters.management.cattle.io local -o yaml 2>/dev/null | grep -i 'server\|url' | head
